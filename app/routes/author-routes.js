// const router = require('express').Router();
// const authorController = require('../controllers/author-controller.js');


// router.get("/", authorController.findAll);

// router.get("/:authorId", authorController.findOne);

// router.put("/:authorId", authorController.update);

// router.delete("/:authorId", authorController.delete);

// router.delete("/", authorController.deleteAll);

// module.exports = router;
